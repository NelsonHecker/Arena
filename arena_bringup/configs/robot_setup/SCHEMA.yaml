/**:
  ros__parameters:
    robots:
    - agent: AGENT_NAME
      amount: NUMBER
      model: ROBOT_MODEL
      planner: LOCAL_PLANNER
      navigator: nav2
      # capabilities:
      #   - kind: nav2
      #   - kind: none
