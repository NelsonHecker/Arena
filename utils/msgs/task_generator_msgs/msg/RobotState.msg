# A resolved, live fleet member.
RobotDescriptor descriptor         # lean identity, still shared with RobotQueue
RobotCap[] caps                    # resolved effective caps + bound adapters
diagnostic_msgs/KeyValue[] params  # resolved morphology params
